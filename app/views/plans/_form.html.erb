<%= simple_form_for(@plan) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :completed %>
    <%= f.association :user %>
    <%= f.association :tip %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
