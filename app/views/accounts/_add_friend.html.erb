<%= form_for(current_user.relationships.build(friendee_id:@account.user.id), remote: true) do |f| %>
	<div><%= f.hidden_field :friendee_id %></div>
	<%= f.submit "Add friend", class: "btn btn-large btn-primary" %>
<% end %>

